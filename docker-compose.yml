version: '3.8'

services:
  proptrex-bot:
    build: .
    container_name: proptrex-signal-bot
    restart: unless-stopped
    expose:
      - "8000"
    networks:
      - coolify

networks:
  coolify:
    external: true
